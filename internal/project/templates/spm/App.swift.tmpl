import SwiftUI

@main
struct {{.Name}}App: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {
            Image(systemName: "swift")
                .font(.system(size: 64))
                .foregroundStyle(.orange)
            Text("{{.Name}}")
                .font(.title)
                .fontWeight(.semibold)
            Text("built with xless")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
